---
import { Image } from 'astro:assets';
import headshot from '../assets/images/profile/headshot.jpg';
import ContourCanvas from './ContourCanvas.astro';
---

<section class="hero">
  <ContourCanvas />
  <div class="hero-content container">
    <div class="hero-main">

    <div class="hero-text">
      <h1 class="hero-title">
        <span class="title-line"><span class="gradient-primary">Digital Transformation</span> & <span class="gradient-secondary">Civic Technology</span></span>
        <span class="gradient-text">Executive</span>
      </h1>
      <p class="hero-subtitle">
        <span class="subtitle-line-1">Building data-driven platforms</span> <span class="subtitle-line-2">that turn policy into impact</span>
      </p>
      <p class="hero-description">
        A proven leader transforming how New Jersey government serves its residents through innovative digital solutions. With experience spanning academic research, civic innovation, and state government leadership, I've delivered platforms that have processed tens of thousands of public records requests, protected tenants from eviction, and ensured transparency in lead-contaminated properties—demonstrating that technology can be a powerful force for equity and accountability in public service.
      </p>
      <div class="hero-actions">
        <a href="/portfolio" class="btn btn-primary">View Portfolio</a>
        <a href="/contact" class="btn btn-secondary">Get in Touch</a>
      </div>
    </div>

    <div class="hero-image">
      <div class="headshot-frame">
        <Image
          src={headshot}
          alt="Professional headshot of Gavin Rozzi"
          class="headshot"
          width={400}
          height={400}
          loading="eager"
          fetchpriority="high"
        />
      </div>
      <p class="headshot-caption">Gavin Rozzi</p>
      <p class="headshot-title">Director, Data Center Administration</p>
    </div>
    </div>

    <div class="hero-expertise">
      <div class="expertise-left">
        <h2 class="expertise-headline">
          Policy alone doesn't change lives - <span class="gradient-text">execution does</span>
        </h2>
        <p class="expertise-vision">
          Every successful government initiative requires someone who can translate ambitious mandates into working systems that actually serve residents. I build the technical infrastructure that turns policy aspirations into measurable outcomes.
        </p>

        <h4 class="framework-label">My Execution Framework</h4>

        <div class="impact-diagram">
          <div class="impact-stage">
            <div class="stage-icon">📋</div>
            <h3 class="stage-title">Policy Challenge</h3>
            <p class="stage-description">Complex government mandates requiring technical implementation</p>
          </div>

          <div class="stage-connector">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="url(#gradient1)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <defs>
                <linearGradient id="gradient1" x1="0%" y1="0%" x2="100%" y2="0%">
                  <stop offset="0%" style="stop-color:#f97316;stop-opacity:1" />
                  <stop offset="100%" style="stop-color:#14b8a6;stop-opacity:1" />
                </linearGradient>
              </defs>
            </svg>
          </div>

          <div class="impact-stage">
            <div class="stage-icon">⚙️</div>
            <h3 class="stage-title">Technical Solution</h3>
            <p class="stage-description">Modern frameworks and data infrastructure built for scale</p>
          </div>

          <div class="stage-connector">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="url(#gradient2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <defs>
                <linearGradient id="gradient2" x1="0%" y1="0%" x2="100%" y2="0%">
                  <stop offset="0%" style="stop-color:#f97316;stop-opacity:1" />
                  <stop offset="100%" style="stop-color:#14b8a6;stop-opacity:1" />
                </linearGradient>
              </defs>
            </svg>
          </div>

          <div class="impact-stage">
            <div class="stage-icon">📊</div>
            <h3 class="stage-title">Measurable Impact</h3>
            <p class="stage-description">Analytics and performance tracking built into every platform to drive improvement</p>
          </div>
        </div>
      </div>

      <div class="expertise-right">
        <div class="why-work-section">
          <h3 class="why-work-title">Why Work With Me</h3>
          <p class="why-work-text">
            I bridge the gap between policy objectives and technical execution, bringing a rare combination of
            government operational knowledge and modern development expertise. I understand compliance requirements,
            budget constraints, and the urgency of serving vulnerable populations.
          </p>
        </div>

        <div class="career-timeline">
          <div class="timeline-item">
            <div class="timeline-marker"></div>
            <div class="timeline-content">
              <div class="timeline-year">2025-2026</div>
              <div class="timeline-title">Professional Development</div>
              <div class="timeline-desc">Certified Public Manager (CPM) Program Candidate</div>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker"></div>
            <div class="timeline-content">
              <div class="timeline-year">2024-Present</div>
              <div class="timeline-title">Government Leadership</div>
              <div class="timeline-desc">Founding Director, DHCR Data Center at NJ Department of Community Affairs</div>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker"></div>
            <div class="timeline-content">
              <div class="timeline-year">2020-2022</div>
              <div class="timeline-title">Academic Research</div>
              <div class="timeline-desc">Founding Data Scientist, Rutgers Urban & Civic Informatics Lab; Researcher, William J. Hughes Center for Public Policy</div>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker"></div>
            <div class="timeline-content">
              <div class="timeline-year">2017-2024</div>
              <div class="timeline-title">Civic Innovation</div>
              <div class="timeline-desc">Founded OPRAmachine, NJ's first statewide FOIA platform; Board member, NJ Foundation for Open Government</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="hero-background"></div>
</section>

<style>
  .hero {
    position: relative;
    padding: var(--space-16) 0 var(--space-12);
    overflow: hidden;
    min-height: 80vh;
    display: flex;
    align-items: center;
  }

  .hero-background {
    position: absolute;
    inset: 0;
    background: var(--gradient-hero);
    opacity: 0.1;
    z-index: -1;
  }

  .hero-background::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 20% 30%, rgba(249, 115, 22, 0.15) 0%, transparent 50%),
      radial-gradient(circle at 80% 70%, rgba(20, 184, 166, 0.15) 0%, transparent 50%);
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero-main {
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: var(--space-8);
    align-items: center;
    margin-bottom: var(--space-8);
  }

  .hero-text {
    max-width: 800px;
    min-width: 0;
  }

  @supports (width: clamp(300px, 100%, 800px)) {
    .hero-text {
      width: clamp(300px, 100%, 800px);
    }
  }

  .hero-image {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: var(--space-3);
  }

  .headshot-frame {
    position: relative;
    width: 320px;
    height: 320px;
    border-radius: 50%;
    padding: 6px;
    background: var(--gradient-primary);
    box-shadow: var(--shadow-lg);
    transition: transform var(--transition-base), box-shadow var(--transition-base);
  }

  .headshot-frame:hover {
    transform: scale(1.02);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  .headshot {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
    background-color: var(--color-neutral-100);
    border: 4px solid white;
  }

  .headshot-caption {
    font-family: var(--font-heading);
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    letter-spacing: -0.02em;
    margin: 0 0 var(--space-1) 0;
    text-align: center;
    background: linear-gradient(135deg, #f97316 0%, #ea580c 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .headshot-caption::selection {
    -webkit-text-fill-color: var(--color-text-primary);
    background-color: var(--color-primary-light);
  }

  .headshot-caption::-moz-selection {
    -webkit-text-fill-color: var(--color-text-primary);
    color: var(--color-text-primary);
    background-color: var(--color-primary-light);
  }

  .headshot-title {
    font-family: var(--font-body);
    font-size: var(--text-base);
    font-weight: var(--font-bold);
    color: var(--color-text-secondary);
    margin: 0;
    text-align: center;
    line-height: var(--leading-relaxed);
  }

  .hero-title {
    font-size: var(--text-5xl);
    font-weight: var(--font-bold);
    line-height: var(--leading-tight);
    margin-bottom: var(--space-4);
    letter-spacing: -0.02em;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .title-line {
    display: inline;
    white-space: nowrap;
  }

  .gradient-primary {
    background: linear-gradient(135deg, #f97316 0%, #ea580c 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .gradient-primary::selection {
    -webkit-text-fill-color: var(--color-text-primary);
    background-color: var(--color-primary-light);
  }

  .gradient-primary::-moz-selection {
    -webkit-text-fill-color: var(--color-text-primary);
    color: var(--color-text-primary);
    background-color: var(--color-primary-light);
  }

  .gradient-secondary {
    background: linear-gradient(135deg, #14b8a6 0%, #0d9488 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .gradient-secondary::selection {
    -webkit-text-fill-color: var(--color-text-primary);
    background-color: var(--color-secondary-light);
  }

  .gradient-secondary::-moz-selection {
    -webkit-text-fill-color: var(--color-text-primary);
    color: var(--color-text-primary);
    background-color: var(--color-secondary-light);
  }

  .hero-subtitle {
    font-size: var(--text-2xl);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-4);
    font-weight: var(--font-medium);
    hyphens: manual;
    word-break: keep-all;
    max-width: 750px;
    line-height: 1.5;
  }

  .subtitle-line-1,
  .subtitle-line-2 {
    display: inline;
    white-space: nowrap;
  }

  .subtitle-line-1::after {
    content: ' ';
    white-space: pre-wrap;
  }

  .hero-description {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin-bottom: var(--space-6);
    max-width: 750px;
    hyphens: manual;
    word-break: normal;
    overflow-wrap: anywhere;
    text-wrap: pretty;
    letter-spacing: 0.01em;
  }

  .hero-actions {
    display: flex;
    gap: var(--space-3);
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-3) var(--space-5);
    font-family: var(--font-heading);
    font-size: var(--text-base);
    font-weight: var(--font-medium);
    text-decoration: none;
    border-radius: var(--radius-lg);
    transition: all var(--transition-base);
    cursor: pointer;
    border: 2px solid transparent;
  }

  .btn-primary {
    background: var(--gradient-primary);
    color: white;
    box-shadow: var(--shadow-md);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .btn-secondary {
    background-color: transparent;
    color: var(--color-text-primary);
    border-color: var(--color-neutral-300);
  }

  .btn-secondary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
    background-color: rgba(249, 115, 22, 0.05);
  }

  .hero-expertise {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: var(--space-8);
    max-width: 1100px;
    margin-top: var(--space-8);
  }

  .expertise-left {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .expertise-headline {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    line-height: var(--leading-tight);
    color: var(--color-text-primary);
    margin-bottom: 0;
  }

  .expertise-vision {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
    max-width: 650px;
  }

  .framework-label {
    font-family: var(--font-heading);
    font-size: 0.9375rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #f97316;
    text-align: center;
    margin: var(--space-3) 0 var(--space-3) 0;
    line-height: var(--leading-tight);
  }

  .impact-diagram {
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
    padding: var(--space-6);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(255, 255, 255, 0.95) 100%);
    backdrop-filter: blur(10px);
    border: 1px solid var(--color-neutral-200);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
  }

  .impact-stage {
    flex: 1;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .stage-icon {
    font-size: var(--text-4xl);
    margin-bottom: var(--space-2);
  }

  .stage-title {
    font-size: var(--text-lg);
    font-weight: var(--font-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--space-2);
  }

  .stage-description {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .stage-connector {
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 52px;
  }

  .expertise-right {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .why-work-section {
    background: linear-gradient(135deg, rgba(249, 115, 22, 0.08) 0%, rgba(20, 184, 166, 0.08) 100%);
    border: 1px solid var(--color-neutral-200);
    border-radius: var(--radius-xl);
    padding: var(--space-5);
  }

  .why-work-title {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--space-3);
  }

  .why-work-text {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .career-timeline {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .timeline-item {
    display: flex;
    gap: var(--space-3);
    position: relative;
  }

  .timeline-item:not(:last-child)::after {
    content: '';
    position: absolute;
    left: 11px;
    top: 32px;
    bottom: -16px;
    width: 2px;
    background: linear-gradient(180deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    opacity: 0.3;
  }

  .timeline-marker {
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    border-radius: var(--radius-full);
    background: var(--gradient-primary);
    border: 3px solid white;
    box-shadow: var(--shadow-md);
    position: relative;
    z-index: 1;
  }

  .timeline-content {
    flex: 1;
    padding-top: 2px;
  }

  .timeline-year {
    font-size: var(--text-xs);
    font-weight: var(--font-bold);
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--space-1);
  }

  .timeline-title {
    font-size: var(--text-base);
    font-weight: var(--font-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--space-1);
  }

  .timeline-desc {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }

  @media (max-width: 1024px) {
    .hero-main {
      gap: var(--space-6);
    }

    .hero-expertise {
      gap: var(--space-6);
    }
  }

  @media (max-width: 968px) {
    .hero-main {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .hero-image {
      order: -1;
    }

    .headshot-frame {
      width: 280px;
      height: 280px;
    }
  }

  @media (max-width: 820px) {
    .hero-expertise {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .expertise-right {
      order: -1;
    }

    .impact-diagram {
      flex-direction: column;
      gap: var(--space-4);
      padding: var(--space-5);
    }

    .stage-connector {
      transform: rotate(90deg);
      margin: 0 auto;
      align-self: center;
    }
  }

  @media (max-width: 1360px) {
    .hero-text {
      max-width: 700px;
    }
  }

  @media (max-width: 1280px) {
    .hero-text {
      max-width: 650px;
    }

    .hero-title {
      font-size: clamp(2rem, 4vw, 3rem);
    }

    .hero-subtitle {
      font-size: clamp(1.25rem, 2.5vw, 1.5rem);
    }
  }

  @media (max-width: 1200px) {
    .hero-text {
      max-width: 600px;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: var(--space-12) 0 var(--space-8);
      min-height: auto;
    }

    .headshot-frame {
      width: 240px;
      height: 240px;
    }

    .headshot-caption {
      font-size: var(--text-2xl);
    }

    .headshot-title {
      font-size: var(--text-sm);
    }

    .hero-title {
      font-size: var(--text-4xl);
    }

    .title-line {
      white-space: normal;
    }

    .hero-subtitle {
      font-size: var(--text-xl);
    }

    .subtitle-line-1,
    .subtitle-line-2 {
      white-space: normal;
    }

    .hero-description {
      font-size: var(--text-xl);
      line-height: 1.75;
      letter-spacing: 0.015em;
      max-width: 100%;
    }

    .hero-actions {
      flex-direction: column;
      align-items: flex-start;
    }

    .btn {
      width: 100%;
      max-width: 320px;
    }

    .expertise-headline {
      font-size: var(--text-2xl);
    }

    .impact-diagram {
      padding: var(--space-4);
    }

    .stage-title {
      font-size: var(--text-base);
    }

    .stage-description {
      font-size: var(--text-xs);
    }

    .timeline-item {
      gap: var(--space-2);
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding: var(--space-10) 0 var(--space-6);
    }

    .hero-text {
      max-width: 100%;
    }

    .hero-description {
      font-size: var(--text-lg);
      line-height: 1.8;
      letter-spacing: 0.02em;
    }

    .headshot-frame {
      width: 200px;
      height: 200px;
    }

    .headshot-caption {
      font-size: var(--text-xl);
    }

    .headshot-title {
      font-size: var(--text-xs);
    }

    .hero-title {
      font-size: var(--text-3xl);
    }

    .hero-subtitle {
      font-size: var(--text-lg);
    }

    .stage-icon {
      font-size: var(--text-3xl);
      margin-bottom: var(--space-1);
    }

    .expertise-headline {
      font-size: var(--text-xl);
    }

    .framework-label {
      font-size: 0.875rem;
      margin: var(--space-2) 0 var(--space-2) 0;
    }

    .impact-diagram {
      padding: var(--space-3);
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: var(--space-8) 0 var(--space-6);
    }

    .hero-description {
      font-size: var(--text-base);
      line-height: 1.8;
    }

    .headshot-frame {
      width: 180px;
      height: 180px;
    }

    .headshot-caption {
      font-size: var(--text-lg);
    }

    .headshot-title {
      font-size: var(--text-xs);
    }

    .hero-title {
      font-size: var(--text-2xl);
    }

    .btn {
      max-width: 100%;
      font-size: var(--text-sm);
      padding: var(--space-2) var(--space-4);
    }
  }

  @media (max-width: 360px) {
    .headshot-frame {
      width: 160px;
      height: 160px;
    }

    .hero-title {
      font-size: var(--text-xl);
    }

    .stage-title {
      font-size: var(--text-sm);
    }
  }
</style>
